<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mercado La Fontane – Guásimas, Cuba</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --cream: #f5f0e8;
    --dark: #1a1410;
    --brown: #6b4c2a;
    --gold: #c8973a;
    --rust: #c0522a;
    --light: #faf7f2;
    --card: #ffffff;
    --border: #e8dfd0;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--cream);
    color: var(--dark);
    min-height: 100vh;
  }

  /* HEADER */
  header {
    background: var(--dark);
    padding: 0;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 2px 20px rgba(0,0,0,0.3);
  }

  .header-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 18px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .logo {
    display: flex;
    flex-direction: column;
    line-height: 1;
  }

  .logo-main {
    font-family: 'Playfair Display', serif;
    font-size: 1.6rem;
    font-weight: 900;
    color: var(--gold);
    letter-spacing: -0.5px;
  }

  .logo-sub {
    font-size: 0.65rem;
    color: #888;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-top: 2px;
  }

  .admin-btn {
    background: transparent;
    border: 1px solid #444;
    color: #888;
    padding: 8px 16px;
    border-radius: 6px;
    font-size: 0.75rem;
    letter-spacing: 1px;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.2s;
    font-family: 'DM Sans', sans-serif;
  }

  .admin-btn:hover { border-color: var(--gold); color: var(--gold); }

  /* HERO */
  .hero {
    background: var(--dark);
    padding: 60px 24px 50px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(ellipse at center, rgba(200,151,58,0.08) 0%, transparent 60%);
    pointer-events: none;
  }

  .hero-tag {
    display: inline-block;
    background: rgba(200,151,58,0.15);
    color: var(--gold);
    font-size: 0.7rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    padding: 6px 16px;
    border-radius: 20px;
    border: 1px solid rgba(200,151,58,0.3);
    margin-bottom: 20px;
  }

  .hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 6vw, 3.5rem);
    color: #fff;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 14px;
  }

  .hero h1 span { color: var(--gold); }

  .hero p {
    color: #999;
    font-size: 0.95rem;
    max-width: 400px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* FILTROS */
  .filters {
    max-width: 1100px;
    margin: 36px auto 0;
    padding: 0 24px;
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
  }

  .filter-btn {
    background: var(--card);
    border: 1.5px solid var(--border);
    color: var(--brown);
    padding: 8px 18px;
    border-radius: 30px;
    font-size: 0.82rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
    font-family: 'DM Sans', sans-serif;
  }

  .filter-btn:hover, .filter-btn.active {
    background: var(--dark);
    border-color: var(--dark);
    color: var(--gold);
  }

  /* GALERÍA */
  .gallery-section {
    max-width: 1100px;
    margin: 36px auto 0;
    padding: 0 24px;
  }

  .gallery-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 18px;
  }

  .gallery-title {
    font-family: 'Playfair Display', serif;
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--dark);
  }

  .gallery-title span {
    display: block;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.75rem;
    color: #aaa;
    font-weight: 400;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 4px;
  }

  .gallery-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    grid-template-rows: 200px 200px;
    gap: 10px;
    border-radius: 16px;
    overflow: hidden;
  }

  .gallery-item {
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, #e8ddd0, #d4c8b8);
    cursor: pointer;
  }

  .gallery-item:first-child {
    grid-row: 1 / 3;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease;
  }

  .gallery-item:hover img { transform: scale(1.05); }

  .gallery-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 8px;
    color: #b8a898;
    font-size: 0.78rem;
    text-align: center;
    padding: 16px;
  }

  .gallery-placeholder .ph-icon { font-size: 2rem; opacity: 0.6; }

  .gallery-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.5) 0%, transparent 50%);
    opacity: 0;
    transition: opacity 0.3s;
    display: flex;
    align-items: flex-end;
    padding: 14px;
  }

  .gallery-item:hover .gallery-overlay { opacity: 1; }

  .gallery-overlay-text {
    color: #fff;
    font-size: 0.78rem;
    font-weight: 500;
  }

  /* Lightbox */
  .lightbox {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.92);
    z-index: 500;
    align-items: center;
    justify-content: center;
  }

  .lightbox.open { display: flex; }

  .lightbox img {
    max-width: 90vw;
    max-height: 85vh;
    border-radius: 10px;
    object-fit: contain;
  }

  .lightbox-close {
    position: absolute;
    top: 20px;
    right: 20px;
    background: rgba(255,255,255,0.15);
    border: none;
    color: #fff;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    font-size: 1.2rem;
    cursor: pointer;
  }

  /* Admin galería */
  .gallery-admin-note {
    font-size: 0.75rem;
    color: #bbb;
    margin-top: 10px;
    text-align: center;
  }

  .admin-gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    margin-top: 14px;
  }

  .admin-gallery-item {
    position: relative;
    border-radius: 8px;
    overflow: hidden;
    height: 90px;
    background: var(--light);
    border: 1.5px dashed var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 1.5rem;
    color: #ccc;
    transition: border-color 0.2s;
  }

  .admin-gallery-item:hover { border-color: var(--gold); }

  .admin-gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .admin-gallery-item .remove-img {
    position: absolute;
    top: 4px;
    right: 4px;
    background: rgba(0,0,0,0.6);
    color: #fff;
    border: none;
    border-radius: 50%;
    width: 22px;
    height: 22px;
    font-size: 0.7rem;
    cursor: pointer;
    display: none;
    align-items: center;
    justify-content: center;
  }

  .admin-gallery-item:hover .remove-img { display: flex; }

  @media (max-width: 600px) {
    .gallery-grid {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 160px 160px 160px;
    }
    .gallery-item:first-child { grid-row: 1; grid-column: 1 / 3; }
  }

  /* GRID */
  .catalog {
    max-width: 1100px;
    margin: 30px auto 60px;
    padding: 0 24px;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 20px;
  }

  .product-card {
    background: var(--card);
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid var(--border);
    transition: transform 0.2s, box-shadow 0.2s;
    position: relative;
  }

  .product-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0,0,0,0.1);
  }

  .product-card.hidden { display: none; }

  .card-img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    background: linear-gradient(135deg, #f0e8da, #e8ddd0);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    position: relative;
  }

  .card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .card-img .emoji-placeholder {
    font-size: 3.5rem;
  }

  .card-category {
    position: absolute;
    top: 10px;
    left: 10px;
    background: var(--dark);
    color: var(--gold);
    font-size: 0.62rem;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    padding: 4px 10px;
    border-radius: 20px;
  }

  .card-body {
    padding: 16px;
  }

  .card-name {
    font-family: 'Playfair Display', serif;
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--dark);
    margin-bottom: 6px;
    line-height: 1.3;
  }

  .card-desc {
    font-size: 0.8rem;
    color: #888;
    line-height: 1.5;
    margin-bottom: 14px;
  }

  .card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .card-price {
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--rust);
  }

  .card-price span {
    font-size: 0.7rem;
    color: #aaa;
    font-weight: 400;
    margin-left: 3px;
  }

  .card-available {
    font-size: 0.7rem;
    color: #5a9a5a;
    background: #edf7ed;
    padding: 3px 10px;
    border-radius: 20px;
  }

  .card-available.no { color: #c0522a; background: #fdf0ec; }

  /* FOOTER */
  footer {
    background: var(--dark);
    color: #666;
    text-align: center;
    padding: 30px 24px;
    font-size: 0.8rem;
    line-height: 1.8;
  }

  footer strong { color: var(--gold); }

  /* ADMIN MODAL */
  .modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.7);
    z-index: 1000;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .modal-overlay.open { display: flex; }

  .modal {
    background: #fff;
    border-radius: 18px;
    width: 100%;
    max-width: 680px;
    max-height: 90vh;
    overflow-y: auto;
    padding: 32px;
    position: relative;
  }

  .modal-title {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    color: var(--dark);
    margin-bottom: 6px;
  }

  .modal-sub {
    font-size: 0.82rem;
    color: #999;
    margin-bottom: 28px;
  }

  .close-modal {
    position: absolute;
    top: 20px;
    right: 20px;
    background: #f0f0f0;
    border: none;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    font-size: 1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* LOGIN */
  .login-form { display: flex; flex-direction: column; gap: 14px; }

  .form-group { display: flex; flex-direction: column; gap: 6px; }

  .form-group label {
    font-size: 0.78rem;
    font-weight: 500;
    color: #555;
    letter-spacing: 0.5px;
  }

  .form-group input, .form-group select, .form-group textarea {
    border: 1.5px solid var(--border);
    border-radius: 8px;
    padding: 10px 14px;
    font-size: 0.9rem;
    font-family: 'DM Sans', sans-serif;
    outline: none;
    transition: border-color 0.2s;
    color: var(--dark);
  }

  .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
    border-color: var(--gold);
  }

  .form-group textarea { resize: vertical; min-height: 80px; }

  .btn-primary {
    background: var(--dark);
    color: var(--gold);
    border: none;
    padding: 12px 24px;
    border-radius: 8px;
    font-size: 0.88rem;
    font-weight: 500;
    cursor: pointer;
    font-family: 'DM Sans', sans-serif;
    transition: opacity 0.2s;
    letter-spacing: 0.5px;
  }

  .btn-primary:hover { opacity: 0.85; }

  .btn-danger {
    background: #fdf0ec;
    color: var(--rust);
    border: 1px solid #f0c4b4;
    padding: 7px 14px;
    border-radius: 6px;
    font-size: 0.78rem;
    cursor: pointer;
    font-family: 'DM Sans', sans-serif;
  }

  .btn-edit {
    background: #f0f4ff;
    color: #3355cc;
    border: 1px solid #c4cef0;
    padding: 7px 14px;
    border-radius: 6px;
    font-size: 0.78rem;
    cursor: pointer;
    font-family: 'DM Sans', sans-serif;
  }

  /* ADMIN PANEL */
  .admin-tabs {
    display: flex;
    gap: 8px;
    margin-bottom: 24px;
    border-bottom: 2px solid var(--border);
    padding-bottom: 0;
  }

  .tab-btn {
    background: none;
    border: none;
    padding: 10px 16px;
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    color: #999;
    border-bottom: 2px solid transparent;
    margin-bottom: -2px;
    font-family: 'DM Sans', sans-serif;
    transition: all 0.2s;
  }

  .tab-btn.active { color: var(--dark); border-bottom-color: var(--gold); }

  .tab-content { display: none; }
  .tab-content.active { display: block; }

  /* PRODUCT LIST IN ADMIN */
  .admin-product-list { display: flex; flex-direction: column; gap: 10px; margin-top: 16px; }

  .admin-product-item {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 12px 16px;
    background: var(--light);
    border-radius: 10px;
    border: 1px solid var(--border);
  }

  .admin-product-item .item-emoji { font-size: 1.8rem; }

  .admin-product-item .item-info { flex: 1; }

  .admin-product-item .item-name {
    font-weight: 600;
    font-size: 0.9rem;
    color: var(--dark);
  }

  .admin-product-item .item-cat {
    font-size: 0.75rem;
    color: #999;
  }

  .admin-product-item .item-actions { display: flex; gap: 8px; }

  /* FORM ADD PRODUCT */
  .add-product-form {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
    margin-top: 16px;
  }

  .add-product-form .full { grid-column: 1 / -1; }

  .img-preview {
    width: 100%;
    height: 120px;
    object-fit: cover;
    border-radius: 8px;
    border: 1.5px solid var(--border);
    display: none;
    margin-top: 8px;
  }

  .error-msg {
    color: var(--rust);
    font-size: 0.82rem;
    margin-top: 8px;
    display: none;
  }

  /* WHATSAPP */
  .wa-float {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 200;
    background: #25D366;
    color: #fff;
    border: none;
    border-radius: 50px;
    padding: 14px 20px;
    font-size: 0.88rem;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 8px;
    box-shadow: 0 4px 20px rgba(37,211,102,0.4);
    text-decoration: none;
    font-family: 'DM Sans', sans-serif;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .wa-float:hover { transform: translateY(-2px); box-shadow: 0 8px 28px rgba(37,211,102,0.5); }
  .wa-float svg { width: 22px; height: 22px; fill: #fff; }

  .wa-share {
    background: #25D366;
    color: #fff;
    border: none;
    border-radius: 6px;
    padding: 7px 12px;
    font-size: 0.75rem;
    font-weight: 500;
    cursor: pointer;
    font-family: 'DM Sans', sans-serif;
    display: flex;
    align-items: center;
    gap: 5px;
    transition: opacity 0.2s;
  }
  .wa-share:hover { opacity: 0.85; }

  @media (max-width: 600px) {
    .add-product-form { grid-template-columns: 1fr; }
    .add-product-form .full { grid-column: 1; }
    .catalog { grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); gap: 14px; }
    .card-img { height: 140px; }
  }
</style>
</head>
<body>

<!-- HEADER -->
<header>
  <div class="header-inner">
    <div class="logo">
      <span class="logo-main">La Fontane</span>
      <span class="logo-sub">Guásimas · Cuba</span>
    </div>
    <button class="admin-btn" onclick="openAdmin()">⚙ Admin</button>
  </div>
</header>

<!-- HERO -->
<div class="hero">
  <div class="hero-tag">📍 Calle Caimán, Guásimas</div>
  <h1>Mercado<br><span>La Fontane</span></h1>
  <p>Productos frescos y variados para el hogar. Consulta disponibilidad y precios actualizados.</p>
</div>

<!-- FILTROS -->
<div class="filters" id="filters"></div>

<!-- GALERÍA NUESTRO MERCADO -->
<div class="gallery-section" id="gallerySection">
  <div class="gallery-header">
    <div class="gallery-title">
      <span>📸 Así somos</span>
      Nuestro Mercado
    </div>
  </div>
  <div class="gallery-grid" id="galleryGrid"></div>
  <p class="gallery-admin-note" id="galleryNote" style="display:none">Agrega fotos desde el panel de administración → pestaña 📸 Galería</p>
</div>

<!-- Lightbox -->
<div class="lightbox" id="lightbox">
  <button class="lightbox-close" onclick="closeLightbox()">✕</button>
  <img id="lightboxImg" src="" alt="">
</div>

<!-- CATÁLOGO -->
<div class="catalog" id="catalog"></div>

<!-- FOOTER -->
<footer>
  <strong>Mercado La Fontane</strong><br>
  Calle Caimán · Guásimas, Cuba<br>
  Precios en CUP · Sujetos a disponibilidad
</footer>

<!-- WHATSAPP FLOTANTE -->
<a class="wa-float" href="https://wa.me/5359382320" target="_blank">
  <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z"/><path d="M12 0C5.373 0 0 5.373 0 12c0 2.127.558 4.122 1.532 5.859L.057 23.428a.75.75 0 0 0 .915.915l5.569-1.475A11.943 11.943 0 0 0 12 24c6.627 0 12-5.373 12-12S18.627 0 12 0zm0 21.75a9.713 9.713 0 0 1-4.953-1.355l-.355-.211-3.664.97.983-3.595-.23-.368A9.718 9.718 0 0 1 2.25 12C2.25 6.615 6.615 2.25 12 2.25S21.75 6.615 21.75 12 17.385 21.75 12 21.75z"/></svg>
  Escríbenos
</a>

<!-- MODAL -->
<div class="modal-overlay" id="modalOverlay">
  <div class="modal" id="modalBox">
    <!-- Login -->
    <div id="loginView">
      <button class="close-modal" onclick="closeAdmin()">✕</button>
      <div class="modal-title">Panel de Administración</div>
      <div class="modal-sub">Acceso solo para el administrador del mercado.</div>
      <div class="login-form">
        <div class="form-group">
          <label>CONTRASEÑA</label>
          <input type="password" id="pwInput" placeholder="Ingresa la contraseña" onkeydown="if(event.key==='Enter')doLogin()">
        </div>
        <div class="error-msg" id="loginError">Contraseña incorrecta.</div>
        <button class="btn-primary" onclick="doLogin()">Entrar</button>
      </div>
    </div>

    <!-- Panel -->
    <div id="adminView" style="display:none">
      <button class="close-modal" onclick="closeAdmin()">✕</button>
      <div class="modal-title">Panel de Administración</div>
      <div class="modal-sub">Gestiona los productos del catálogo.</div>

      <div class="admin-tabs">
        <button class="tab-btn active" onclick="switchTab('list', this)">📋 Productos</button>
        <button class="tab-btn" onclick="switchTab('add', this)">➕ Agregar</button>
        <button class="tab-btn" onclick="switchTab('gallery', this)">📸 Galería</button>
        <button class="tab-btn" onclick="switchTab('settings', this)">🔑 Contraseña</button>
      </div>

      <!-- TAB: Lista -->
      <div class="tab-content active" id="tab-list">
        <div class="admin-product-list" id="adminList"></div>
      </div>

      <!-- TAB: Agregar -->
      <div class="tab-content" id="tab-add">
        <div class="add-product-form">
          <div class="form-group full">
            <label>NOMBRE DEL PRODUCTO</label>
            <input type="text" id="newName" placeholder="Ej: Jabón de baño">
          </div>
          <div class="form-group">
            <label>CATEGORÍA</label>
            <select id="newCat">
              <option value="aseo">🧴 Aseo</option>
              <option value="enlatados">🥫 Enlatados</option>
              <option value="refrescos">🥤 Refrescos</option>
              <option value="bebidas">🍶 Bebidas</option>
              <option value="confituras">🍬 Confituras</option>
              <option value="gym">💪 Gym</option>
            </select>
          </div>
          <div class="form-group">
            <label>PRECIO (CUP)</label>
            <input type="number" id="newPrice" placeholder="Ej: 150">
          </div>
          <div class="form-group full">
            <label>DESCRIPCIÓN</label>
            <textarea id="newDesc" placeholder="Descripción breve del producto..."></textarea>
          </div>
          <div class="form-group full">
            <label>FOTO (opcional)</label>
            <input type="file" id="newImg" accept="image/*" onchange="previewImg()">
            <img id="imgPreview" class="img-preview">
          </div>
          <div class="form-group">
            <label>DISPONIBLE</label>
            <select id="newAvail">
              <option value="si">✅ Sí</option>
              <option value="no">❌ No</option>
            </select>
          </div>
          <div class="full">
            <button class="btn-primary" onclick="addProduct()" style="width:100%">Guardar producto</button>
          </div>
        </div>
      </div>

      <!-- TAB: Galería -->
      <div class="tab-content" id="tab-gallery">
        <p style="font-size:0.85rem;color:#888;margin-top:4px">Sube hasta 5 fotos del mercado. Se muestran en la sección "Nuestro Mercado".</p>
        <div class="admin-gallery-grid" id="adminGalleryGrid"></div>
        <button class="btn-primary" style="margin-top:16px;width:100%" onclick="saveGallery()">Guardar galería</button>
      </div>

      <!-- TAB: Contraseña -->
      <div class="tab-content" id="tab-settings">
        <div class="login-form" style="margin-top:16px">
          <div class="form-group">
            <label>CONTRASEÑA ACTUAL</label>
            <input type="password" id="pwOld" placeholder="Contraseña actual">
          </div>
          <div class="form-group">
            <label>NUEVA CONTRASEÑA</label>
            <input type="password" id="pwNew" placeholder="Nueva contraseña">
          </div>
          <div class="form-group">
            <label>CONFIRMAR</label>
            <input type="password" id="pwNew2" placeholder="Repetir contraseña">
          </div>
          <div class="error-msg" id="pwError"></div>
          <button class="btn-primary" onclick="changePassword()">Cambiar contraseña</button>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
// ── DATOS ──────────────────────────────────────────────
const STORAGE_KEY = 'fontane_products';
const PW_KEY = 'fontane_pw';

const categoryEmoji = {
  aseo: '🧴', enlatados: '🥫', refrescos: '🥤',
  bebidas: '🍶', confituras: '🍬', gym: '💪'
};

const categoryLabel = {
  aseo: 'Aseo', enlatados: 'Enlatados', refrescos: 'Refrescos',
  bebidas: 'Bebidas', confituras: 'Confituras', gym: 'Gym'
};

const defaultProducts = [
  { id: 1, name: 'Jabón de baño', cat: 'aseo', price: 120, desc: 'Jabón suave para uso diario.', img: '', avail: 'si' },
  { id: 2, name: 'Detergente en polvo', cat: 'aseo', price: 200, desc: 'Para ropa y limpieza del hogar.', img: '', avail: 'si' },
  { id: 3, name: 'Pasta dental', cat: 'aseo', price: 90, desc: 'Con flúor, menta fresca.', img: '', avail: 'si' },
  { id: 4, name: 'Atún en lata', cat: 'enlatados', price: 180, desc: 'Atún en aceite, lata 170g.', img: '', avail: 'si' },
  { id: 5, name: 'Conserva de tomate', cat: 'enlatados', price: 95, desc: 'Tomate natural triturado.', img: '', avail: 'no' },
  { id: 6, name: 'Refresco de cola', cat: 'refrescos', price: 75, desc: 'Botella 500ml fría.', img: '', avail: 'si' },
  { id: 7, name: 'Jugo de naranja', cat: 'refrescos', price: 60, desc: 'Néctar de fruta natural.', img: '', avail: 'si' },
  { id: 8, name: 'Cerveza', cat: 'bebidas', price: 130, desc: 'Lata 330ml bien fría.', img: '', avail: 'si' },
  { id: 9, name: 'Ron cubano', cat: 'bebidas', price: 450, desc: 'Ron añejo, botella 750ml.', img: '', avail: 'si' },
  { id: 10, name: 'Caramelos surtidos', cat: 'confituras', price: 50, desc: 'Bolsa variada 200g.', img: '', avail: 'si' },
  { id: 11, name: 'Chocolate en tableta', cat: 'confituras', price: 140, desc: 'Tableta 100g, cacao cubano.', img: '', avail: 'si' },
  { id: 12, name: 'Proteína en polvo', cat: 'gym', price: 1200, desc: 'Whey protein, sabor vainilla 1kg.', img: '', avail: 'si' },
  { id: 13, name: 'Creatina', cat: 'gym', price: 900, desc: 'Monohidrato, 300g.', img: '', avail: 'no' },
  { id: 14, name: 'Multivitamínico', cat: 'gym', price: 350, desc: 'Frasco 60 cápsulas.', img: '', avail: 'si' },
];

function getProducts() {
  const raw = localStorage.getItem(STORAGE_KEY);
  return raw ? JSON.parse(raw) : defaultProducts;
}

function saveProducts(p) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(p));
}

function getPassword() {
  return localStorage.getItem(PW_KEY) || 'ludmila80';
}

// ── RENDER CATÁLOGO ────────────────────────────────────
let activeFilter = 'todos';

function renderCatalog(filter) {
  const products = getProducts();
  const catalog = document.getElementById('catalog');
  catalog.innerHTML = '';

  const filtered = filter === 'todos' ? products : products.filter(p => p.cat === filter);

  if (filtered.length === 0) {
    catalog.innerHTML = '<p style="color:#aaa;font-size:0.9rem;grid-column:1/-1;text-align:center;padding:40px 0">No hay productos en esta categoría.</p>';
    return;
  }

  filtered.forEach(p => {
    const card = document.createElement('div');
    card.className = 'product-card';
    card.innerHTML = `
      <div class="card-img">
        ${p.img ? `<img src="${p.img}" alt="${p.name}">` : `<span class="emoji-placeholder">${categoryEmoji[p.cat]}</span>`}
        <span class="card-category">${categoryLabel[p.cat]}</span>
      </div>
      <div class="card-body">
        <div class="card-name">${p.name}</div>
        <div class="card-desc">${p.desc}</div>
        <div class="card-footer">
          <div class="card-price">${p.price.toLocaleString('es-CU')}<span>CUP</span></div>
          <span class="card-available ${p.avail === 'no' ? 'no' : ''}">${p.avail === 'si' ? 'Disponible' : 'Agotado'}</span>
        </div>
        <button class="wa-share" style="margin-top:12px;width:100%;justify-content:center" onclick="shareWA('${p.name}', ${p.price}, '${p.avail}')">📲 Consultar por WhatsApp</button>
      </div>
    `;
    catalog.appendChild(card);
  });
}

function renderFilters() {
  const products = getProducts();
  const cats = [...new Set(products.map(p => p.cat))];
  const container = document.getElementById('filters');
  container.innerHTML = '';

  const all = document.createElement('button');
  all.className = 'filter-btn active';
  all.textContent = '🛒 Todos';
  all.onclick = () => setFilter('todos', all);
  container.appendChild(all);

  cats.forEach(cat => {
    const btn = document.createElement('button');
    btn.className = 'filter-btn';
    btn.textContent = `${categoryEmoji[cat]} ${categoryLabel[cat]}`;
    btn.onclick = () => setFilter(cat, btn);
    container.appendChild(btn);
  });
}

function setFilter(cat, btn) {
  activeFilter = cat;
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  renderCatalog(cat);
}

// ── ADMIN ──────────────────────────────────────────────
function openAdmin() {
  document.getElementById('modalOverlay').classList.add('open');
  document.getElementById('loginView').style.display = 'block';
  document.getElementById('adminView').style.display = 'none';
  document.getElementById('loginError').style.display = 'none';
  document.getElementById('pwInput').value = '';
}

function closeAdmin() {
  document.getElementById('modalOverlay').classList.remove('open');
}

function doLogin() {
  const pw = document.getElementById('pwInput').value;
  if (pw === getPassword()) {
    document.getElementById('loginView').style.display = 'none';
    document.getElementById('adminView').style.display = 'block';
    renderAdminList();
  } else {
    document.getElementById('loginError').style.display = 'block';
  }
}

function switchTab(name, btn) {
  document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
  document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
  btn.classList.add('active');
  document.getElementById('tab-' + name).classList.add('active');
  if (name === 'list') renderAdminList();
  if (name === 'gallery') renderAdminGallery();
}

function renderAdminList() {
  const products = getProducts();
  const list = document.getElementById('adminList');
  list.innerHTML = '';
  products.forEach(p => {
    const item = document.createElement('div');
    item.className = 'admin-product-item';
    item.innerHTML = `
      <span class="item-emoji">${categoryEmoji[p.cat]}</span>
      <div class="item-info">
        <div class="item-name">${p.name} — ${p.price.toLocaleString('es-CU')} CUP</div>
        <div class="item-cat">${categoryLabel[p.cat]} · ${p.avail === 'si' ? '✅ Disponible' : '❌ Agotado'}</div>
      </div>
      <div class="item-actions">
        <button class="btn-edit" onclick="editProduct(${p.id})">Editar</button>
        <button class="btn-danger" onclick="deleteProduct(${p.id})">Borrar</button>
      </div>
    `;
    list.appendChild(item);
  });
}

function deleteProduct(id) {
  if (!confirm('¿Eliminar este producto?')) return;
  const products = getProducts().filter(p => p.id !== id);
  saveProducts(products);
  renderAdminList();
  renderCatalog(activeFilter);
  renderFilters();
}

function editProduct(id) {
  const products = getProducts();
  const p = products.find(x => x.id === id);
  if (!p) return;

  // Switch to add tab and pre-fill
  document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
  document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.tab-btn')[1].classList.add('active');
  document.getElementById('tab-add').classList.add('active');

  document.getElementById('newName').value = p.name;
  document.getElementById('newCat').value = p.cat;
  document.getElementById('newPrice').value = p.price;
  document.getElementById('newDesc').value = p.desc;
  document.getElementById('newAvail').value = p.avail;

  if (p.img) {
    const preview = document.getElementById('imgPreview');
    preview.src = p.img;
    preview.style.display = 'block';
  }

  // Remove old and save as new
  const btn = document.querySelector('#tab-add .btn-primary');
  btn.textContent = 'Actualizar producto';
  btn.onclick = () => {
    const updated = getProducts().filter(x => x.id !== id);
    const newP = buildProductFromForm(id);
    if (!newP) return;
    updated.push(newP);
    // Sort by id
    updated.sort((a,b) => a.id - b.id);
    saveProducts(updated);
    renderCatalog(activeFilter);
    renderFilters();
    btn.textContent = 'Guardar producto';
    btn.onclick = addProduct;
    switchTab('list', document.querySelectorAll('.tab-btn')[0]);
  };
}

function buildProductFromForm(id) {
  const name = document.getElementById('newName').value.trim();
  const cat = document.getElementById('newCat').value;
  const price = parseInt(document.getElementById('newPrice').value);
  const desc = document.getElementById('newDesc').value.trim();
  const avail = document.getElementById('newAvail').value;
  const img = document.getElementById('imgPreview').src.startsWith('data:') ? document.getElementById('imgPreview').src : '';

  if (!name || isNaN(price)) {
    alert('Por favor completa el nombre y el precio.');
    return null;
  }
  return { id: id || Date.now(), name, cat, price, desc, avail, img };
}

function addProduct() {
  const p = buildProductFromForm(Date.now());
  if (!p) return;
  const products = getProducts();
  products.push(p);
  saveProducts(products);
  renderCatalog(activeFilter);
  renderFilters();

  // Reset form
  document.getElementById('newName').value = '';
  document.getElementById('newPrice').value = '';
  document.getElementById('newDesc').value = '';
  document.getElementById('newImg').value = '';
  document.getElementById('imgPreview').style.display = 'none';
  document.getElementById('newAvail').value = 'si';

  switchTab('list', document.querySelectorAll('.tab-btn')[0]);
}

function previewImg() {
  const file = document.getElementById('newImg').files[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = e => {
    const preview = document.getElementById('imgPreview');
    preview.src = e.target.result;
    preview.style.display = 'block';
  };
  reader.readAsDataURL(file);
}

function changePassword() {
  const old = document.getElementById('pwOld').value;
  const n1 = document.getElementById('pwNew').value;
  const n2 = document.getElementById('pwNew2').value;
  const err = document.getElementById('pwError');

  if (old !== getPassword()) {
    err.textContent = 'La contraseña actual es incorrecta.';
    err.style.display = 'block'; return;
  }
  if (n1.length < 4) {
    err.textContent = 'La nueva contraseña debe tener al menos 4 caracteres.';
    err.style.display = 'block'; return;
  }
  if (n1 !== n2) {
    err.textContent = 'Las contraseñas no coinciden.';
    err.style.display = 'block'; return;
  }
  localStorage.setItem(PW_KEY, n1);
  err.style.display = 'none';
  alert('✅ Contraseña cambiada correctamente.');
  document.getElementById('pwOld').value = '';
  document.getElementById('pwNew').value = '';
  document.getElementById('pwNew2').value = '';
}

// Cerrar modal al hacer clic fuera
document.getElementById('modalOverlay').addEventListener('click', function(e) {
  if (e.target === this) closeAdmin();
});

// ── GALERÍA ────────────────────────────────────────────
const GALLERY_KEY = 'fontane_gallery';
const GALLERY_LABELS = ['Atendiendo clientes', 'Nuestros productos', 'El local', 'Variedad del día', 'Equipo La Fontane'];

function getGallery() {
  const raw = localStorage.getItem(GALLERY_KEY);
  return raw ? JSON.parse(raw) : [];
}

function renderGallery() {
  const photos = getGallery();
  const grid = document.getElementById('galleryGrid');
  const note = document.getElementById('galleryNote');
  grid.innerHTML = '';

  const placeholders = [
    { icon: '🏪', label: 'El local' },
    { icon: '🛒', label: 'Productos frescos' },
    { icon: '👐', label: 'Atendiendo' },
    { icon: '📦', label: 'Variedad' },
    { icon: '✨', label: 'Novedades' },
  ];

  const total = 5;
  for (let i = 0; i < total; i++) {
    const item = document.createElement('div');
    item.className = 'gallery-item';
    if (i === 0) item.style.gridRow = '1 / 3';

    if (photos[i]) {
      item.innerHTML = `
        <img src="${photos[i]}" alt="Foto ${i+1}" onclick="openLightbox('${photos[i]}')">
        <div class="gallery-overlay"><span class="gallery-overlay-text">${GALLERY_LABELS[i] || ''}</span></div>
      `;
    } else {
      item.innerHTML = `
        <div class="gallery-placeholder">
          <span class="ph-icon">${placeholders[i].icon}</span>
          <span>${placeholders[i].label}</span>
        </div>
      `;
    }
    grid.appendChild(item);
  }

  note.style.display = photos.length === 0 ? 'block' : 'none';
}

function renderAdminGallery() {
  const photos = getGallery();
  const grid = document.getElementById('adminGalleryGrid');
  grid.innerHTML = '';

  for (let i = 0; i < 5; i++) {
    const item = document.createElement('div');
    item.className = 'admin-gallery-item';

    if (photos[i]) {
      item.innerHTML = `
        <img src="${photos[i]}" alt="Foto ${i+1}">
        <button class="remove-img" onclick="removeGalleryPhoto(${i})">✕</button>
      `;
    } else {
      item.innerHTML = `<span>＋</span>`;
      item.onclick = () => pickGalleryPhoto(i);
    }
    grid.appendChild(item);
  }
}

function pickGalleryPhoto(index) {
  const input = document.createElement('input');
  input.type = 'file';
  input.accept = 'image/*';
  input.onchange = e => {
    const file = e.target.files[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = ev => {
      const photos = getGallery();
      photos[index] = ev.target.result;
      localStorage.setItem(GALLERY_KEY, JSON.stringify(photos));
      renderAdminGallery();
      renderGallery();
    };
    reader.readAsDataURL(file);
  };
  input.click();
}

function removeGalleryPhoto(index) {
  const photos = getGallery();
  photos.splice(index, 1);
  localStorage.setItem(GALLERY_KEY, JSON.stringify(photos));
  renderAdminGallery();
  renderGallery();
}

function saveGallery() {
  renderGallery();
  alert('✅ Galería guardada.');
}

function openLightbox(src) {
  document.getElementById('lightboxImg').src = src;
  document.getElementById('lightbox').classList.add('open');
}

function closeLightbox() {
  document.getElementById('lightbox').classList.remove('open');
}

document.getElementById('lightbox').addEventListener('click', function(e) {
  if (e.target === this) closeLightbox();
});

// ── WHATSAPP ───────────────────────────────────────────
function shareWA(name, price, avail) {
  const estado = avail === 'si' ? 'disponible' : 'agotado';
  const msg = `Hola, vi en el catálogo de Mercado La Fontane que *${name}* está a *${price.toLocaleString('es-CU')} CUP* (${estado}). ¿Pueden confirmarme disponibilidad?`;
  window.open(`https://wa.me/5359382320?text=${encodeURIComponent(msg)}`, '_blank');
}

// ── INIT ───────────────────────────────────────────────
renderFilters();
renderCatalog('todos');
renderGallery();
</script>
</body>
</html>
